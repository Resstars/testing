class Lesson {
    constructor(title, text, video, audio, quiz) {
        this.title = title;
        this.text = text;
        this.video = video;
        this.audio = audio;
        this.quiz = quiz;
    }
}