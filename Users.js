class User {
    constructor(name, login, email, password, role, courses, progress) {
      this.name = name;
      this.login = login;
      this.email = email;
      this.password = password;
      this.role = role;
      this.courses = courses;
      this.progress = progress;
    }
  }
